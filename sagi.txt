sudo apt-get install git gcc make curl

xcape
$ sudo apt-get install pkg-config libx11-dev libxtst-dev libxi-dev

$ git clone https://github.com/alols/xcape.git
$ cd xcape
$ make
$ sudo make install

xkb-switch
sudo apt-get install libxkbfile-dev

$ mkdir build && cd build
$ cmake ..
$ make
$ sudo make install


sudo apt-get install zsh
chsh -s $(which zsh)
  reload terminal

oh-my-zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"


vim
sudo apt-get install vim-gtk3

neovim
fonts for vim


sudo apt-get install tmux
$ git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
prefix + I / install all plugins


sudo apt-get install ack-grep
sudo apt-get install exuberant-ctags

sudo apt-get install 
sudo apt-get install 



System tweaks


Mate Tweak
  Panel
    Enable advanced menu
    Redmond / save layout


Software & Updates / Updates tab
  all / immediately / daily
  for any new Ubuntu version
